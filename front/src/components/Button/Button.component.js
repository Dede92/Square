import React from 'react';
import { Button } from '@material-ui/core';

const ButtonComponent = () => {
  return <Button variant="contained" color="primary">Show occupancy</Button>;
}

export default ButtonComponent;